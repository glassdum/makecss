[package]
name = "makecss-core"
version = "0.1.0"
edition = "2021"
description = "CSS 문법으로 네이티브 UI를 그리는 엔진의 코어. 파싱 → 스타일 → 레이아웃 → 페인트."

# 주목할 점: 의존성(dependencies)이 '비어 있습니다'.
# 이 엔진은 외부 라이브러리 없이 순수 Rust 표준 라이브러리만으로 동작합니다.
# 그래서 어떤 마법도 숨어있지 않고, 모든 단계를 우리가 직접 볼 수 있습니다.
[dependencies]
